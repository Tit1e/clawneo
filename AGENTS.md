# AGENTS

## 调试规则

- 当用户明确说“配置已经生效”或贴出了当前配置内容时，不要先假设是用户配置错误。必须先检查代码里是否存在“配置被默认值、内置模型或环境覆盖”的路径，再继续做协议、代理或上游兼容性分析。
- 当实际运行结果与配置显示不一致时，优先打印并核对运行时最终生效值，例如 `model`、`provider`、`api`、`baseUrl`、认证来源，而不是连续修改上层业务逻辑。
- 对同一问题做最小可行试验时，每一步都要先确认“当前请求是否真的打到了预期地址和预期协议”，确认后再继续下一步。
